# WSL2 Linux Web Server Lab

WSL2 Ubuntu上に Apache / PHP / MySQL のLAMP環境を自動構築する学習用IaCリポジトリです。

## 目的

手作業で理解したLinux Webサーバー構築手順を、分隊型シェルスクリプトとして再現可能にします。

## 実行方法

```bash
cp .env.example .env
./scripts/run.sh
```

